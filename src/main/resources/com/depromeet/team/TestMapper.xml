<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.depromeet.team.mapper.TestMapper">

    <select id="now" resultType="java.time.LocalDateTime">
		SELECT NOW() FROM DUAL
	</select>
</mapper>