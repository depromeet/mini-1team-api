<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.depromeet.team.mapper.MemberInfoMapper">
	<insert id="regist">
		insert into MEMBER (name, age, gender, nickname, keyword) values (#{name}, #{age}, #{gender}, #{nickname}, #{keyword})
	</insert>
	
	<select id="showInfo" resultType="MemberInfoVO">
		select * from MEMBER where uid = #{uid}
	</select>
	
	<update id="modify">
		update MEMBER set keyword = #{keyword} where uid = #{uid}
	</update>
	
	<delete id="remove">
		delete from MEMBER where uid = #{uid}
	</delete>
</mapper>